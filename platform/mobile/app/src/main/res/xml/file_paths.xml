<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Where downloaded APK updates are cached before handing to the installer. -->
    <cache-path name="updates" path="updates/" />
    <external-cache-path name="external_updates" path="updates/" />
</paths>
